

def train_model(model, optimizer, criterion, num_epoch, trainloader, testloader, save_name):
    """[summary]

    Args:
        model (PyTorch nn.Module): PyTorch neural network architecture
        optimizer (PyTorch optimizer): optimization method
        criterion (function): loss function
        num_epoch (int): number of epochs
        trainloader (PyTorch dataloader): dataloader for trainset
        testloader (PyTorch dataloader): dataloader for testset
        save_name (string): name under which to save trained model and results
    """
    pass